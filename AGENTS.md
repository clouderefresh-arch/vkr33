# AGENTS.md

## Cursor Cloud specific instructions

This repository (`vkr33`) is currently empty — it contains only a README placeholder. It was created as a workspace for a Russian graduation thesis (ВКР — Выпускная Квалификационная Работа).

### Current state
- No application code, no dependencies, no build/test/lint system.
- No services to start.

### If document processing is added later
If Python scripts for document processing (e.g., python-docx, pdf2image) are added:
- Install dependencies with `pip install -r requirements.txt`
- The VM has Python 3.12 available by default.

### Notes
- The user's primary task involves editing a diploma thesis using files from Google Drive. Those files must be downloaded/uploaded manually or via `gdown`/Google Drive API.
